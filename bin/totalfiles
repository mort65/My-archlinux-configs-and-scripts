#!/bin/bash
#Counts number of files in current directory or a path
PT=$(pwd)
if [ "${#}" -gt 1 ]; then
    echo -e $red"Error:$reset Invalid number of parameters"
elif [ "${#}" -eq 1 ]; then
    PT=$1
fi
expr `/bin/ls -f "${PT}" | wc -l` - 2
